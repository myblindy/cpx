import { StateToImagePipe } from './state-to-image.pipe';

describe('StateToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new StateToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
